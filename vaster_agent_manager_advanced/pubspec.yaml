name: vaster_agent_manager_advanced
description: Advanced AgentManager supervisor engine featuring supervisor trees, parallel task routing, lifecycle states, and event bus integration.
version: 1.0.0

resolution: workspace

environment:
  sdk: ^3.13.0-282.1.beta

dependencies:
  vaster_agent_basic:
  vaster_agent_manager:
  vaster_context_manager:
  vaster_events:
  vaster_model:
  vaster_session_manager:
  vaster_tool_manager:

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
  vaster_model_fake:
