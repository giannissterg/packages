name: vaster_agent_messaging
description: Asynchronous inter-agent messaging queue and inbox management for Vaster.
version: 0.2.0

resolution: workspace

environment:
  sdk: ^3.13.0-282.1.beta

dependencies:

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
