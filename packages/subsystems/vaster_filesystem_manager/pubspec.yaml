name: vaster_filesystem_manager
description: Active virtual filesystem manager, mount routing, transactions, and context bridging for Vaster.
version: 0.4.0

resolution: workspace

environment:
  sdk: ^3.13.0-282.1.beta

dependencies:
  vaster_context:
  vaster_filesystem:

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
  vaster_filesystem_memory:
