name: vaster_agent_manager
description: Pure AgentManager abstract interface and lifecycle definitions for Vaster.
version: 0.5.0

resolution: workspace

environment:
  sdk: ^3.13.0-282.1.beta

dependencies:
  vaster_agent:
  vaster_context_manager:
  vaster_model:
  vaster_tool_manager:

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
