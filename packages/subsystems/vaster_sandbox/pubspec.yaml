name: vaster_sandbox
description: Pure isolated code sandbox interfaces, descriptors, security policies, requests, and results.
version: 0.4.0

resolution: workspace

environment:
  sdk: ^3.13.0-282.1.beta

dependencies:
  vaster_cancellation:

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
