name: vaster_agent
description: Pure agent interfaces, descriptors, tasks, and outputs for Vaster.
version: 0.4.0

resolution: workspace

environment:
  sdk: ^3.13.0-282.1.beta

dependencies:
  vaster_agent_descriptor:
  vaster_model:
  vaster_policy:
  vaster_session:

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
