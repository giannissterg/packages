name: vaster_context
description: Virtual context primitives, heaps, regions, priorities, token budgets, and context sources for Vaster.
version: 0.4.0

resolution: workspace

environment:
  sdk: ^3.13.0-282.1.beta

dependencies:
  crypto: ^3.0.3
  vaster_model:
  vaster_token_estimate:

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
