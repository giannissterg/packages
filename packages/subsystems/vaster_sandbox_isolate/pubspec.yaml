name: vaster_sandbox_isolate
description: Dart Isolate code sandbox backend implementation.
version: 0.5.0

resolution: workspace

environment:
  sdk: ^3.13.0-282.1.beta

dependencies:
  vaster_cancellation:
  vaster_sandbox:

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
