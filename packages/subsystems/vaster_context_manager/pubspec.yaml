name: vaster_context_manager
description: Active context manager, allocation strategy execution, region eviction, and dynamic context compilation for Vaster.
version: 0.4.0

resolution: workspace

environment:
  sdk: ^3.13.0-282.1.beta

dependencies:
  vaster_context:
  vaster_events:
  vaster_model:
  vaster_token_estimate:

dev_dependencies:
  vaster_model_fake:
    path: ../../backends/vaster_model_fake
  lints: ^6.0.0
  test: ^1.25.6
