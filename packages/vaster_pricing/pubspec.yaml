name: vaster_pricing
description: Monetary pricing for Vaster — per-token rate tables and cost resolution for model usage. Wire-reported cost always wins over computed rates.
version: 0.4.0

resolution: workspace

environment:
  sdk: ^3.13.0-282.1.beta

dependencies:
  vaster_model:

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
