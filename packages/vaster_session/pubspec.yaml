name: vaster_session
description: Model conversation session state, turn history, and execution binding for Vaster.
version: 0.2.0

resolution: workspace

environment:
  sdk: ^3.13.0-282.1.beta

dependencies:
  vaster_context:
  vaster_context_manager:
  vaster_model:
  vaster_token_estimate:

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
  vaster_model_fake:
