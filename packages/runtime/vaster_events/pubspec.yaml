name: vaster_events
description: Unified event bus and runtime event hierarchy for Vaster.
version: 0.5.0

resolution: workspace

environment:
  sdk: ^3.13.0-282.1.beta

dependencies:

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
