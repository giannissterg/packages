name: vaster_session_manager
description: Multi-session creation, lookup, isolation, and lifecycle management for Vaster.
version: 1.0.0

resolution: workspace

environment:
  sdk: ^3.13.0-282.1.beta

dependencies:
  vaster_context:
  vaster_context_manager:
  vaster_model:
  vaster_session:

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
  vaster_model_fake:
