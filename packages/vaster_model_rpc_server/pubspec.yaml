name: vaster_model_rpc_server
description: Out-of-process Unix Domain Socket RPC server sidecar host for VasterModel.
version: 0.4.0
resolution: workspace
publish_to: none

environment:
  sdk: ^3.6.0

dependencies:
  vaster_model:
    path: ../vaster_model
  vaster_model_google_ai:
    path: ../vaster_model_google_ai
  vaster_model_rpc:
    path: ../vaster_model_rpc

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
