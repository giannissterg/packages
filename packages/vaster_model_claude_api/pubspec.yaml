name: vaster_model_claude_api
description: Compiler-level Claude Messages API backend for the VasterModel interface — typed tool ABI, structured outputs, prompt-cache lowering, and exact token accounting.
version: 0.4.0

resolution: workspace

environment:
  sdk: ^3.13.0-282.1.beta

dependencies:
  http: ^1.2.0
  vaster_model:

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
