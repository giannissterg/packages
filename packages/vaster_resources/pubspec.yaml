name: vaster_resources
description: Resource quotas, token limits, tool call bounds, and execution tracker for Vaster.
version: 0.3.0

resolution: workspace

environment:
  sdk: ^3.13.0-282.1.beta

dependencies:

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
