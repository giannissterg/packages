name: vaster_model_rpc
description: Out-of-process Unix Domain Socket RPC client implementation of VasterModel interface.
version: 0.3.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.6.0

dependencies:
  vaster_model:
    path: ../vaster_model

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
  vaster_model_rpc_server:
    path: ../vaster_model_rpc_server
