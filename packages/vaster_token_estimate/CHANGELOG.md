# Changelog

## 0.2.0

- Initial version: `TokenEstimate.forText` / `forMessages` / `forExchange`,
  extracted from ~10 scattered inline `~/ 4` heuristics across the workspace
  so estimates are consistent and always labeled `UsageSource.estimated`.
