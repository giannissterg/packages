name: vaster_token_estimate
description: Heuristic token estimation for Vaster — the single home for length-based token counts, always labeled as estimates.
version: 0.3.0

resolution: workspace

environment:
  sdk: ^3.13.0-282.1.beta

dependencies:
  vaster_model:

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
