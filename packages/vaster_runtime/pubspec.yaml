name: vaster_runtime
description: Concrete execution runtime executing low-level Vaster ISA instructions on VasterVirtualMachine.
version: 0.2.0

resolution: workspace

environment:
  sdk: ^3.13.0-282.1.beta

dependencies:
  vaster_pricing:
  vaster_agent_manager_advanced:
  vaster_sandbox:
  vaster_policy_engine:
  vaster_filesystem_memory:
  vaster_filesystem_local:
  vaster_filesystem:
  vaster_context_manager:
  vaster_context:
  vaster_agent_messaging:
  vaster_agent:
  vaster_budget:
  vaster_events:
  vaster_instruction:
  vaster_model:
  vaster_token_estimate:
  vaster_policy:
  vaster_resources:
  vaster_scheduler:
  vaster_tool:
  vaster_vm_api:

dev_dependencies:
  vaster_vm:
  lints: ^6.0.0
  test: ^1.25.6
  vaster_continuation:
  vaster_continuation_manager:
  vaster_model_fake:
