/// The serializable agent descriptor — identity, role, and capability
/// whitelist. A leaf value object consumed by both the agent runtime and
/// the ISA (CreateAgentOp), keeping vaster_instruction's closure minimal.
library;

export 'src/agent_descriptor.dart';
