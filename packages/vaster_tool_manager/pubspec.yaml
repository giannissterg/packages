name: vaster_tool_manager
description: Tool registry, execution dispatcher, and model tool binding for Vaster.
version: 0.2.0

resolution: workspace

environment:
  sdk: ^3.13.0-282.1.beta

dependencies:
  vaster_model:
  vaster_tool:

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
