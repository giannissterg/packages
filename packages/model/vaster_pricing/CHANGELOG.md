# Changelog

## 0.2.0

- Initial version: `ModelPricing` (per-MTok rates with cache read/write
  splits), `PricingCatalog` (longest-prefix model lookup, dated builtin
  rates, `withOverrides`), and `resolveCostUsd` (wire-reported cost wins
  over computed rates).
