name: vaster_continuation_manager
description: Multi-continuation lifecycle management and durable snapshot storage for Vaster VM.
version: 0.4.0

resolution: workspace

environment:
  sdk: ^3.13.0-282.1.beta

dependencies:
  vaster_continuation:
  vaster_instruction:
  vaster_model:
  vaster_runtime:

dev_dependencies:
  vaster_machine_state:
  lints: ^6.0.0
  test: ^1.25.6
  vaster_model_fake:
  vaster_vm:
