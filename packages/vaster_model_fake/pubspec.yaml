name: vaster_model_fake
description: Fake model backend implementation for testing and development in the Vaster LLM runtime.
version: 0.2.0

resolution: workspace

environment:
  sdk: ^3.13.0-282.1.beta

dependencies:
  vaster_model:

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
