name: vaster_model
description: Provider-agnostic model interfaces, messages, parts, and declarations for Vaster.
version: 1.0.0

resolution: workspace

environment:
  sdk: ^3.13.0-282.1.beta

dependencies:

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
  vaster_model_fake:
