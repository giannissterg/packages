name: vaster_scheduler
description: Deadline-aware priority scheduling, time-slicing, and task orchestration for Vaster VM.
version: 0.4.0

resolution: workspace

environment:
  sdk: ^3.13.0-282.1.beta

dependencies:
  vaster_budget:

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
