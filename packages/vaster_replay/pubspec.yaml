name: vaster_replay
description: Time-Travel Debugging & Deterministic Execution Replay Engine for Vaster LLM Virtual Machine workflows.
version: 0.2.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.6.0

dependencies:
  vaster_filesystem:
    path: ../vaster_filesystem
  vaster_instruction:
    path: ../vaster_instruction
  vaster_model:
    path: ../vaster_model
  vaster_runtime:
    path: ../vaster_runtime
  vaster_vm:
    path: ../vaster_vm

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
  vaster_budget:
    path: ../vaster_budget
  vaster_model_fake:
    path: ../vaster_model_fake
  vaster_policy:
    path: ../vaster_policy
  vaster_scheduler:
    path: ../vaster_scheduler
