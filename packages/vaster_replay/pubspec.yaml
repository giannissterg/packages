name: vaster_replay
description: Deterministic execution recording & replay for Vaster — step journals and model I/O tapes.
version: 0.4.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.6.0

dependencies:
  vaster_instruction:
  vaster_model:
  vaster_runtime:

dev_dependencies:
  vaster_dis:
  vaster_vm:
  lints: ^6.0.0
  test: ^1.25.0
  vaster_model_fake:
