name: vaster_ast
description: Abstract Syntax Tree (AST) node hierarchy and ComposableNode for Vaster workflow pipelines.
version: 0.5.0

resolution: workspace

environment:
  sdk: ^3.13.0-282.1.beta

dependencies:
  vaster_context:
  vaster_domain:
  vaster_instruction:
  vaster_model:

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
