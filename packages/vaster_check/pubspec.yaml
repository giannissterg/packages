name: vaster_check
description: Static verification for compiled Vaster programs — binding dominance, worst-case cost bounds, and policy proofs, before a single instruction runs.
version: 0.4.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.9.0

dependencies:
  vaster_instruction:
  vaster_model:
  vaster_policy:
  vaster_pricing:
  vaster_token_estimate:

dev_dependencies:
  vaster_agent_descriptor:
  lints: ^6.0.0
  test: ^1.25.0
