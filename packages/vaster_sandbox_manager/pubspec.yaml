name: vaster_sandbox_manager
description: Active sandbox manager, execution allocation, and tool generation for Vaster.
version: 1.0.0

resolution: workspace

environment:
  sdk: ^3.13.0-282.1.beta

dependencies:
  vaster_model:
  vaster_sandbox:
  vaster_tool:

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
  vaster_sandbox_isolate:
