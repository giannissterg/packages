name: vaster_compiler
description: WorkflowCompiler interface and BasicWorkflowCompiler translating vaster_ast Pipeline trees into low-level VasterProgram ISA bytecode.
version: 1.0.0

resolution: workspace

environment:
  sdk: ^3.13.0-282.1.beta

dependencies:
  vaster_agent:
  vaster_ast:
  vaster_domain:
  vaster_instruction:
  vaster_model:
  vaster_resources:
  vaster_sandbox:

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
  vaster_budget:
  vaster_model_fake:
  vaster_policy:
  vaster_runtime:
  vaster_scheduler:
  vaster_vm:
