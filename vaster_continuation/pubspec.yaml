name: vaster_continuation
description: First-class Continuation & Execution Snapshot management for Vaster VM.
version: 1.0.0

resolution: workspace

environment:
  sdk: ^3.13.0-282.1.beta

dependencies:
  vaster_domain:
  vaster_instruction:
  vaster_model:
  vaster_runtime:
  vaster_vm:

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
  vaster_ast:
  vaster_compiler:
  vaster_model_fake:
